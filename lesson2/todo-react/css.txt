В css модулях можно отказаться от бэм, т.к. все изолировано. CSS модули автоматически сгенерируют уникальные названия на этапе сборки
import styles from './Button.module.scss' в styles будет объект со свойствами имена которых соответсвуют классам в файле стилей


нюанс с анимацией: компонент может размонтироваться раньше, чем закончится анимация, а при работе с сервером
легко получить рассинхрон между анимацией и данными (ref и вспомогательная функция usedCombinedRefs), но
на todoitem уже есть ref второй ref просто так не повесить, react его перезапишет. Надо объеденить refs

const useCombinedRefs = (...refs) => {
    return (node) => {
        refs.forEach((ref) => {

            if (!ref) return
            
            if (typeof ref === 'function') {                
                ref(node)
            } else {
                ref.current = node
            }
        })
    }
}

export default useCombinedRefs

const handleClick = () => {
    animationRef.current?.classList.add(styles.isDisappearing)
    setTimeout(() => {
    deleteTask(id)
    }, 300)
}

ref={combinedRef}>


// если ответ от сервера на удаление идет долго, то может быть рассинхрон, наше анимация стартует
до ответа сервера или если запрос завершился ошибочно, то фигня. надо чтобы анимациятолько после положительного
ответа сервера