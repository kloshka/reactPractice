props - способо передавать данные в компонент извне


const ToDoList = () => {
    const hasTasks = true
    if (!hasTasks) {
        return (
            <div className="todo__empty-message"></div>
        )
    }
    return (
      <ul className="todo__list">
        <ToDoItem title="купить молоко" isDone={false}/> 
        // параметры props передаются как атрибуты html
        <li className="todo__item todo-item">
          <input
            className="todo-item__checkbox"
            id="task-2"
            type="checkbox"
          />
          <label
            className="todo-item__label"
            htmlFor="task-2"
          >
            Task 2
          </label>
          <button
            className="todo-item__delete-button"
            aria-label="Delete"
            title="Delete"
          >
            <svg
              width="20"
              height="20"
              viewBox="0 0 20 20"
              fill="none"
              xmlns="http://www.w3.org/2000/svg"
            >
              <path
                d="M15 5L5 15M5 5L15 15"
                stroke="#757575"
                strokeWidth="2"
                strokeLinecap="round"
                strokeLinejoin="round"
              />
            </svg>
          </button>
        </li>
      </ul>
    )\


внутри компонента они приходят как объект
const ToDoItem = (props) => {
    console.log("props", props)
    return (
        <li className="todo__item todo-item">
          <input
            className="todo-item__checkbox"
            id="task-1"
            type="checkbox"
            checked
          />
          <label
            className="todo-item__label"
            htmlFor="task-1"
          >
            Task 1
          </label>
          <button
            className="todo-item__delete-button"
            aria-label="Delete"
            title="Delete"
          >
            <svg
              width="20"
              height="20"
              viewBox="0 0 20 20"
              fill="none"
              xmlns="http://www.w3.org/2000/svg"
            >
              <path
                d="M15 5L5 15M5 5L15 15"
                stroke="#757575"
                strokeWidth="2"
                strokeLinecap="round"
                strokeLinejoin="round"
              />
            </svg>
          </button>
        </li>
    )
}

props передаются только от родителя к ребенку

Компонент не должен изменять переданный props, он их только читает


children - Это специальный Props.
 Это все что находится между открывающим и закрывающим тегом компонента
 <Button>Add</Button>


 const Button = (props) => {
    const {
        children
    } = props
 }